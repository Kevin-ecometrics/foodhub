[install]
# Only install package versions published at least 1 days ago
minimumReleaseAge = 86400 # seconds

# Exclude trusted packages from the age gate
minimumReleaseAgeExcludes = ["@types/node", "typescript"]